MenuResponse:
  type: object
  properties:
    data:
      type: object
      properties:
        items:
          type: array
          description: Массив кодов пунктов меню доступных пользователю
          items:
            type: string
            description: Код (значение из MenuItemCodeEnum)
            $ref: ../enums/menu_item_code_enum.yaml
      required:
        - items
  required:
    - data

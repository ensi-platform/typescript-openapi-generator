type: string
description: |
  Типы фильтров по полям. Расшифровка значений:
    * `default` - Обычный фильтр, соответствующий типу поля
    * `like` - Аналогично default, но по подстроке
    * `many` - Фильтр принимает массив значений
    * `range` - Фильтр по диапазону
enum:
  - default
  - like
  - many
  - range
x-enum-varnames:
  - DEFAULT
  - LIKE
  - MANY
  - RANGE
x-enum-descriptions:
  - Обычный фильтр, соответствующий типу поля
  - Аналогично default, но по подстроке
  - Фильтр принимает массив значений
  - Фильтр по диапазону

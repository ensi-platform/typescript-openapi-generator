/**
 * Не трогать руками, файлы автогенерируемые
 */

/**
 * Information about the file as an object. For the consumer it does not matter what keys are inside
 */
export interface FileProtected {[key: string]: string}
